import styled from 'styled-components';

export const Wrapper = styled.div`
    .wrapper-footer {
        background-color : var(--blue);
        bottom: 0;

        .footer-text {
            font-size: 1rem;
        }
    }
`;  
