import styled from 'styled-components';

export const Wrapper = styled.div`
    .wrapper-navbar {
        background-color : var(--blue);

        .logo-navbar{
            width: 10%;
            height: auto;
        }
    }
`;  
