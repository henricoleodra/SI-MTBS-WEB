import styled from 'styled-components';

export const Wrapper = styled.div`
    .wrapper-pagination {

            
            .active {
                text-decoration: underline;
            }
    }
`;