import styled from "styled-components";

export const Wrapper = styled.div`
  .taginput-wrapper {
    border: solid 1px #46d0fe;
    border-radius: 5px;
    height: 180px;

    .taginput-button {
      color: var(--white);
      margin-left: 5px;
      margin-top: 1px;
    }

    .taginput-button:hover {
      color: #e0e0e0;
      cursor: pointer;
    }
  }
`;
