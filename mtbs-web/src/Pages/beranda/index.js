import React from 'react';
import {
    Layout,
    Beranda 
} from '../../Containers'

const Index = ( props ) => {
    return (
        <Layout step={0}>
            <Beranda/>
        </Layout>
    );
}  

export default Index;